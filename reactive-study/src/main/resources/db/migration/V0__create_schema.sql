-- V0: схема для лабораторных работ reactive-study.
-- Flyway (create-schemas: true) создаёт пустую reactive_study до этой миграции.

CREATE SCHEMA IF NOT EXISTS reactive_study AUTHORIZATION CURRENT_USER;
