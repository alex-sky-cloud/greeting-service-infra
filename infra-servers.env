# IP-адреса серверов. Формат: VAR=1.2.3.4
# В Git Bash НЕ используйте угловые скобки <IP> — символ < ломает source.
# Замените REPLACE_ME на реальные публичные IP после заказа VPS.

USER=sky

DEVTOOLS_IP=188.227.87.57 #201.51.27.38
K3S_SERVER_IP=188.227.87.57
K3S_WORKER_1_IP=93.183.72.78
K3S_WORKER_2_IP=78.140.240.48
STORAGE_1_IP=REPLACE_ME
STORAGE_2_IP=REPLACE_ME

# Внешний IP Traefik (на схеме может совпадать с worker-1 — подставьте свой)
TRAEFIK_ENTRY_IP=REPLACE_ME

# Алиасы имён из гайда (если в командах K8S_MASTER_IP / TRAEFIK_1_IP):
K8S_MASTER_IP=${K3S_SERVER_IP}
TRAEFIK_1_IP=${TRAEFIK_ENTRY_IP}

# K3S_TOKEN пишет scripts/manual-deploy/k3s-master/install-k3s-master.sh
# (не вставляйте руками, не коммитьте). После записи снова:
#   source <(tr -d '\r' < ./infra-servers.env)

# K3S_TOKEN — пишет скрипт k3s, не коммитить
K3S_TOKEN=K10e9898ccc0072fc498de73550b0525317c98ed030a6c8580f92e5d46d8ee330f2::server:7a7bff2bd0207c45ff9eaa0ca301f0e2
