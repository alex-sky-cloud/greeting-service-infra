# IP-адреса серверов. Формат: VAR=1.2.3.4
# В Git Bash НЕ используйте угловые скобки <IP> — символ < ломает source.
# Файл в .gitignore — не коммитить.

USER=sky

# Infrawire Cloud — три VPS для проверки k3s (master + 2 worker)
K3S_SERVER_IP=111.235.151.62
K3S_WORKER_1_IP=212.80.221.132
K3S_WORKER_2_IP=204.11.2.183

DEVTOOLS_IP=212.80.221.195
STORAGE_1_IP=REPLACE_ME
STORAGE_2_IP=REPLACE_ME
TRAEFIK_ENTRY_IP=REPLACE_ME

K8S_MASTER_IP=${K3S_SERVER_IP}
TRAEFIK_1_IP=${TRAEFIK_ENTRY_IP}

# API Infrawire (console.infrawire.net → Settings → API). Не коммитить.
# В веб-панели добавляют SSH-ключ; API-токен — для curl/scripts к api.infrawire.net.
INFRAWIRE_API_TOKEN=0r8hh4meMukzzOGCcfYcknvONmXu5b6ZR7eH1VxOpw20dqfm3nK1ySzhRiB0ZlQY

# K3S_TOKEN пишет install-k3s-master.sh после установки master

# K3S_TOKEN — пишет install-k3s-master.sh после установки master; не коммитить
# K3S_TOKEN=

# K3S_TOKEN — пишет скрипт k3s, не коммитить
K3S_TOKEN=K10ce232834506054e1163a2bd4ce9f44eeb01b1595c4ff2d1dc5a9ba043ae85a02::server:dda0b54a9d841071c86e4b4c1e7600a3
