{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "reactive-demo.serviceAccountName" . }}
  labels:
    {{- include "reactive-demo.labels" . | nindent 4 }}
{{- end }}
