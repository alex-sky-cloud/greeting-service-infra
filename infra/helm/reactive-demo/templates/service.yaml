apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactive-demo.fullname" . }}
  labels:
    {{- include "reactive-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "reactive-demo.selectorLabels" . | nindent 4 }}
