# Разбор вывода `list-resources.sh`

Пример **успешного запуска** в Git Bash:

```bash

cd /d/Project_infra/greeting-service-infra/infra/terraform-serverspace

./scripts/list-resources.sh
```

## Проект

| Поле | Значение |
|------|----------|
| id | **581692** |
| Баланс | 2987.22 RUB |
| Статус | Active |

API-ключ привязан к этому проекту. В панели **Serverspace** должен быть открыт **тот же проект** (id 581692).

---

## Серверы — 4 шт., все Active, локация `ds1` (Москва)

| id | Имя |
|----|-----|
| l44s1304954 | postgres-1 |
| l44s1304955 | gitlab-1 |
| l44s1304956 | k8s-control-plane-1 |
| l44s1304957 | k8s-apps-1 |

Инфраструктура из `main.tf` **создана**. Зависшего «Создание 30%» в API **нет**.

---

## Сети — 2 шт., обе `reactive_net`

| id | Серверы |
|----|---------|
| **l44n753** | все 4 VM |
| **l44n752** | пусто (`servers=-`) |

- **l44n753** — рабочая сеть, к ней подключены все серверы.
- **l44n752** — «хвост» от неудачного apply; в `terraform.tfstate` указана она, а VM уже в **l44n753**. State не совпадает с фактом.

---

## SSH-ключи — 7 шт., все `terraform-key`

Дубликаты от повторных `terraform apply`. В state обычно один id (например **40649**); остальные — мусор в проекте.

---

## Итог

| | |
|---|---|
| VM | 4 из 4 на месте |
| Проблема | 2 сети, 7 SSH-ключей, расхождение state и реальности |
| Что сделать | Удалить сеть `l44n752` и лишние SSH-ключи; `terraform refresh` или `import` |

---

## Связанные файлы

| Файл | Назначение |
|------|------------|
| `list-resources.sh` | Скрипт |
| `list-resources.md` | Как запускать |
| `list-resources-result.md` | Разбор вывода |
