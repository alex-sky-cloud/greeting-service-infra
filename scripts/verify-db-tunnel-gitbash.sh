#!/usr/bin/env bash
# ============================================================================
# verify-db-tunnel-gitbash.sh
#
# НАЗНАЧЕНИЕ: обёртка для обратной совместимости — полная проверка SSH-туннеля к БД.
# ЗАЧЕМ:     старые инструкции ссылались на этот путь; делегирует в 07-verify-all.sh.
# БЕЗОПАСНО: только диагностика, данные на сервере не меняет.
#
# Запуск: bash scripts/verify-db-tunnel-gitbash.sh
# ============================================================================
exec bash "$(dirname "$0")/dev-db-connection/07-verify-all.sh" "$@"